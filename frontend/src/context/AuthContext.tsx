import type { PropsWithChildren } from "react";

export function AuthProvider({ children }: PropsWithChildren) {
  // TODO: Add auth context values after authentication flows are implemented.
  return children;
}

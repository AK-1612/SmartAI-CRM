import {BrainCircuit,LockKeyhole,Mail} from 'lucide-react';import {useForm} from 'react-hook-form';import {Navigate} from 'react-router-dom';import toast from 'react-hot-toast';import {login} from '../api/auth';import {apiError} from '../utils/formatters';
export default function Login(){const {register,handleSubmit,formState:{isSubmitting,errors}}=useForm<{email:string;password:string}>();if(localStorage.getItem('access_token'))return <Navigate to="/"/>;return <main className="grid min-h-screen bg-slate-950 lg:grid-cols-2"><section className="hidden flex-col justify-between overflow-hidden bg-gradient-to-br from-indigo-600 via-violet-700 to-slate-950 p-12 text-white lg:flex"><div className="flex items-center gap-3 text-xl font-bold"><BrainCircuit/>Smart AI CRM</div><div><p className="max-w-xl text-4xl font-bold leading-snug">AI-Powered CRM for Sales, Marketing &amp; Customer Automation</p><p className="mt-5 max-w-lg text-indigo-200">Manage leads, customers, sales pipelines, marketing campaigns, support tickets, and business analytics through intelligent automation.</p></div><p className="text-sm text-indigo-300">No payment required &mdash; sign up and start free</p></section><section className="grid place-items-center p-6"><form onSubmit={handleSubmit(async v=>{try{await login(v.email,v.password);location.href='/'}catch(e){toast.error(apiError(e))}})} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl dark:bg-slate-900"><div className="mb-8"><span className="mb-5 grid h-12 w-12 place-items-center rounded-2xl bg-indigo-600 text-white lg:hidden"><BrainCircuit/></span><h1 className="text-3xl font-bold">Welcome back</h1><p className="mt-2 text-slate-500">Sign in to your Smart AI CRM workspace.</p></div><label><span className="label">Work email</span><div className="relative"><Mail className="absolute left-3 top-3 text-slate-400" size={18}/><input className="input pl-10" type="email" {...register('email',{required:'Email is required'})}/></div>{errors.email&&<small className="text-rose-500">{errors.email.message}</small>}</label><label className="mt-5 block"><span className="label">Password</span><div className="relative"><LockKeyhole className="absolute left-3 top-3 text-slate-400" size={18}/><input className="input pl-10" type="password" {...register('password',{required:'Password is required'})}/></div></label><button disabled={isSubmitting} className="btn-primary mt-7 w-full justify-center">{isSubmitting?'Signing in…':'Sign in'}</button></form></section></main>}

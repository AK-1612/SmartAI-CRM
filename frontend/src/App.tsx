import {Navigate,Route,Routes} from 'react-router-dom';import Layout from './components/Layout';import Dashboard from './pages/Dashboard';import Contacts from './pages/Contacts';import ContactDetails from './pages/ContactDetails';import Activities from './pages/Activities';import AIInsights from './pages/AIInsights';import Login from './pages/Login';
const Guard=({children}:{children:React.ReactNode})=>localStorage.getItem('access_token')?children:<Navigate to="/login"/>;
export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route element={<Guard><Layout/></Guard>}><Route index element={<Dashboard/>}/><Route path="contacts" element={<Contacts/>}/><Route path="contacts/:id" element={<ContactDetails/>}/><Route path="activities" element={<Activities/>}/><Route path="ai" element={<AIInsights/>}/></Route></Routes>}
